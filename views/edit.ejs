<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-5 sm:p-10">
        <!-- Logout Button -->
        <div class="flex justify-end">
            <a class="bg-red-500 px-3 py-2 rounded-md text-sm inline-block" href="/logout">Logout</a>
        </div>

        <!-- Edit Post Section -->
        <h5 class="text-lg mt-5">Edit your post ✍️</h5>
        <form action="/update/<%=post._id%>" method="post" class="space-y-4">
            <!-- Textarea for post content -->
            <textarea placeholder="What's on your mind? 💭"
                class="p-3 outline-none w-full sm:w-2/3 md:w-1/2 lg:w-1/3 resize-none bg-transparent border-4 border-zinc-800 mt-3 rounded-md"
                name="content"><%= post.content %></textarea>

            <!-- Submit button -->
            <input class="px-3 w-full sm:w-40 text-sm py-2 bg-yellow-500 text-black block rounded-md cursor-pointer" type="submit" value="Edit Post">
        </form>
    </div>
</body>

</html>
